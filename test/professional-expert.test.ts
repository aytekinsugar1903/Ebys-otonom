import {describe,it,expect} from "vitest";import {deadlineStatus} from "../src/deadline.js";import {composeOfficialEnvelope,validateOfficialDraft} from "../src/official-language.js";import {hasPermission} from "../src/permissions.js";import type {Actor} from "../src/domain.js";
describe("professional document expert",()=>{it("escalates overdue documents",()=>expect(deadlineStatus("2026-09-18T12:00:00","2026-09-19T12:00:00").level).toBe("OVERDUE"));it("creates reminder ladder",()=>expect(deadlineStatus("2026-09-22T12:00:00","2026-09-19T12:00:00").reminders).toEqual(["72H","24H","4H"]));it("flags speculative language",()=>expect(validateOfficialDraft({subject:"X",body:"Bence uygundur.",warnings:[]})).toContain("SPECULATIVE_LANGUAGE"));it("keeps factual paragraphs",()=>expect(composeOfficialEnvelope({recipient:"A",subject:"Konu",context:"x"},["Bilgi sunulmuştur."]).body).toContain("Bilgi sunulmuştur."));it("enforces permissions",()=>{const a={id:"u" as Actor["id"],roles:[],permissions:[]} satisfies Actor;expect(hasPermission(a,"SIGN")).toBe(false);});});
